version: "3.9"

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      # Persistance des fichiers uploadés et générés entre redémarrages
      - ./uploads:/app/uploads
      - ./output:/app/output
    env_file:
      - .env
    restart: unless-stopped
